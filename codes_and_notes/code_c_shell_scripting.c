/*
gcc -o new_tab c_shell_scripting.c
./new_tab

c execute faster than shell
c is less flexibility than shell

c execute as machine code hence faster
compare to shell or python which is interpretive
usage depends on requirements
*/

#include <stdlib.h>

int main(void){

  system("xfce4-terminal --version");
  system("echo hello world")
  system("xfce-terminal --tab");

  return 0;
}
