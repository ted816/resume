/*
gcc -o new_tab c_shell_scripting.c
./new_tab

c execute faster than shell
less flexibility than shell
*/

#include <stdlib.h>

int main(void){

  system("xfce4-terminal --version");
  system("echo hello world")
  system("xfce-terminal --tab");

  return 0;
}
